let drink = "chai"
// drink = 0 (not allowed bcoz drink is converted into string type)

let cups = Math.random() > 0.5 ? 10 : 5 ; // cups = number(by default)

let chaiFlavour:string = "masala chai"
chaiFlavour = "ginger tea"

let chaiOrder : boolean


